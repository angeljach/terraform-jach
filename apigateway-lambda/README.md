# API Gateway + Lambda
Crea un api gateway que rutea la información provista por la lambda sobre el path /hello. Tomar la salida de la variable "api_gateway_invoke_url" para conocer la ruta a invocar.

Se configuró un throttling burst limit de 5 y rate limit de 10